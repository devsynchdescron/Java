package SectionOne;

public class MathOperations {
    public static void main(String[] args) {
        // Math class is important in Java it provides useful methods

        // Returns minimum of 2 values
        System.out.println(Math.min(2, 10));

        // Returns maximum of 2 values
        System.out.println(Math.max(5, 17));

        // Returns absolute value of a number
        System.out.println(Math.abs(-20));

        // Returns square root of a number
        System.out.println(Math.sqrt(36));

        // Returns x to the power of y, in this case its 2 to the power of 5  
        System.out.println(Math.pow(2, 5));

        // Returns number PI
        System.out.println(Math.PI);

        // Returns a random value between 0.0(inclusive) and 1.0(exclusive)
        System.out.println(Math.random());

        // Lesson exercise : explore more about pre-defined classes and methods in Java
        // Object Class
        // Math Class
        // String Class
        // System Class
        // Random Class
        // Scanner Class
        // Wrapper Class

        // Optional : Make a variable assigned to a random value in range of 0 and 100
        // Hint : What would happen if we try to multiply Math.random() with a variable
    }
}
